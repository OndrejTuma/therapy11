{% set tileWho = entry.tileWho.one() %}

<div class="tile tile-who">
	<div class="front">
		<div class="content">
			<h2>{{ tileWho.title }}</h2>
		</div>
	</div>
	<div class="back">
		<div class="content">
			<h3>{{ tileWho.fullname }}</h3>
			<p>{{ tileWho.linkText }}</p>
		</div>
	</div>
</div>
